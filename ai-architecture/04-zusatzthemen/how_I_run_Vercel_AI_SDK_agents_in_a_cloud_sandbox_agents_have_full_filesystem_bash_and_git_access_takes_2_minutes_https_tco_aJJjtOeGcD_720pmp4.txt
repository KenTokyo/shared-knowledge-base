Ladies and gentlemen, it has never been easier to run AI SDK agents in the cloud in a isolated sandbox with file system and GIT access and everything around it. This is normal AI SDK code. We have a function. In there, we can use the AI SDK as

we always do with any tools, any prompt, anything you want, and let's deploy that to a sandbox. And doing that is really easy. We're gonna create a box with a runtime of Node, and we're gonna pass it our Anthropic key so the AI SDK can actually

use it. First, we're gonna initialize the project. Second, we're gonna set the type to module so our code can actually run properly in Modern JS. And lastly, we're gonna install the dependencies to the sandbox that we need to run the AI SDK,

like AI and the Anthropic adapter. Lastly, we're gonna upload the AI.JS file that we're gonna generate from the TypeScript file to the box to execute it. And that's it. The last thing we need to do, actually, in the sandbox, go ahead box,

execute command, and run that script that we're generating, and we're gonna log the results together here. And that's literally it. We can say TSC, that's TypeScript Compile, and it's gonna generate our JavaScript file we can upload to the box. Let's ??????? index.

That's gonna run the entire script, set up a sandbox. It's gonna install, or it's gonna set up the environment, install the packages, everything we need, and then also, it's gonna run the AI script so the AI SDK called inside of this isolated cloud

sandbox that runs independent of your device. "I am Claude, made by Anthropic. As for which specific version I am, I don't have access to that information." Cool, because Anthropic doesn't give that information. That's not related to our code at all. But what you're seeing, we

just ran the AI SDK in a cloud sandbox with full shell access, GIT, file system, anything you want, in literally less than two minutes.