# Runtime routes — duty-of-tsushima

**Read when:** A hotkey, HUD, or diagnostics request could belong to either V74 Sunken Shrine or Komoda-Strand.
**Status:** Optional tips · measured better solution wins · editing rights: [LEARNING-SYSTEM.md](../../LEARNING-SYSTEM.md)
**Style:** Use compact bullets only; keep one clear fact per bullet. Cut filler, intros, repetition, and needless articles; preserve symptom, cause, action, and evidence.

- **Correct feature lands in the wrong document** — Both page runtimes use F3, so following the current `/` owner ignored that the supplied frame was Komoda-Strand. → Identify the active document from its route link and HTML entry before editing hotkey UI; keep each metric source in that document's frame loop.
  *`4835457` changed three V74 shell files; `a857cdd` restored all three byte-for-byte to `4835457^`; `cc8bcc9` registered one Komoda-only observer and `pnpm check` built both pages from 339 modules · 2026-08-09*
