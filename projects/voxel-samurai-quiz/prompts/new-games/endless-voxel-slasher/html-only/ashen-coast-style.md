# Ashen Coast Style — HTML-Only Chat Prompt

**Use:** Paste this prompt into a chat interface to request one directly downloadable, self-contained HTML game.

```text
Create a complete playable third-person endless voxel action RPG as exactly one self-contained file named `index.html`. Return the file attachment when the chat supports attachments; otherwise return exactly one complete `html` fenced code block and no plan, explanation, setup steps, or second file. The saved file must run by double-click through `file://`. Use only inline HTML, CSS, ordinary JavaScript, GLSL strings, procedural geometry, generated textures/data, SVG/data URIs when needed, and Web Audio. No terminal, CLI, npm, build step, local server, imports, modules, CDN, fetch, network request, external library, font, image, audio, model, worker file, or other asset.

Build one real interactive game with renderer-independent simulation rather than a static mockup. At boot automatically select `WebGL2 → WebGL1 → Canvas 2D`: prefer raw WebGL2, try raw WebGL1 when WebGL2 is unavailable or initialization fails, then run a genuine Canvas 2D renderer when neither GPU path works. Missing WebGL2 must never stop the product. Use a fresh canvas element for each backend attempt because one initialized canvas cannot switch context types. WebGL1 needs its own GLSL ES 1.00-compatible shader/resource path with feature checks; do not feed it `#version 300 es`, WebGL2-only vertex arrays, uniform blocks, integer attributes, 3D textures, or multiple-render-target assumptions. If an initialized GPU context is lost, pause, attempt bounded restoration, then replace the canvas and continue through the fallback ladder without resetting product state. Show a small nonblocking English renderer-status label; show a fatal error only if even Canvas 2D cannot initialize.

All three adapters must consume the same world builders, entities, rig poses, animation timelines, input, UI, collision, combat, skill, wave, and audio state. WebGL renders full 3D. Canvas 2D renders the live game as depth-sorted or isometric faceted forms with visible terrain, full-body silhouettes, camera follow, movement, attacks, telegraphs, projectiles, hit reactions, map contacts, and particles. It may reduce shadows, particles, surface detail, and distant scenery, but it may not replace gameplay with a screenshot, static card, minimap-only view, DOM cube pile, disabled controls, omitted class/skill/enemy/wave, or fake success.

Add an English start screen with two world cards—Skyglass Aqueduct Palace and Verdant Titan Grove Fortress—two class cards—Swordfighter and Elemental Technician—and one Start button. Include a responsive fullscreen canvas, third-person camera, WASD movement, mouse aim, Shift sprint, Space jump, Left Alt dodge, LMB/RMB class attacks, pause/retry/world selection, Health/Stamina/class resource, wave/enemy counters, and nine cooldown slots on Q/E/R/1/2/3/4/5/6. Unlock procedural Web Audio only after user interaction.

Both classes need articulated full-body voxel rigs, planted movement, airborne motion, equipment anchors, hit windows, ground/air Q dash, functional aerial `2`, VFX, audio, map contacts, and equal production depth. Swordfighter uses Spirit and this fixed kit: Q Windstep Sever, E Moon Guard, R Tempest Wheel, 1 Crescent Wave, 2 Skyward Fang, 3 Phantom Blades, 4 Storm Domain, 5 Heaven Splitter, 6 Thousandfold Horizon. Elemental Technician is a technology-based mage using Core Charge, coil gauntlets, reactor, drone, plasma igniters, cryogenic arrays, Tesla channels, vector turbines, induction lance, and gravity lenses. Its fixed kit is Q Ion Vector, E Pyroclast Orb, R Cryo Bastion, 1 Thunder Grid, 2 Cyclone Drive, 3 Magma Rail, 4 Zero-Point Blizzard, 5 Elemental Overdrive, 6 Event Horizon Engine. Make Technician AoE intentionally extreme against groups but keep telegraphs, boss resistance, safe-space readability, pooling, and hard caps.

Run endless waves with exactly ten stone-creature species: Stonefang Wolf, Crystal Mantis, Rootshell Beetle, Shardwing Raven, Obsidian Ape, Runehorn Stag, Boulder Tortoise, Hollow Serpent, Titan Golem, and Storm Wyrm. Every fifth wave 5, 10, 15, … is a mandatory boss wave alternating Titan Golem and Storm Wyrm. Enemies assemble visibly from voxel fragments at authored central anchors before becoming active. Keep navigation, hitboxes, one-hit-per-cast policy, telegraphs, scaling, active-enemy caps, retry reset, and world/class changes functional.

Treat Black Desert only as a quality benchmark for responsiveness, full-body force, layered spectacle, and impact clarity; copy no content. Sword attacks use sampled blade motion, depth-capable mass, HDR core, ribbon, directional arc, contact, debris, pooled light, hit reaction, map response, camera impulse, and spatial audio. Technician effects expose device cause, projectile/beam/wall/field motion, authored AoE edge, material contact, elemental decay, and bounded lights: plasma fireballs, thick fracturing ice walls, valid-target lightning chains, directional wind pressure, magma corridors, blizzards, and a contained gravity-lens Event Horizon. Keep player, targets, telegraphs, and safe space visible.

Build both worlds through a compact single-file AEON-style world engine. Define closed authored JavaScript `WorldSpec` objects for macro landforms, arena, routes, waterways, pads, landmarks, vegetation zones, starts, spawns, materials, light, and camera intent. At startup run one deterministic staged bake into reusable typed arrays: macro shape → erosion/weathering → derived water and channels → graded paths/pads → final height → normals/slope → material/biome fields. Renderer, ground query, collision, structures, vegetation, water, camera, enemies, VFX, and a bounded runtime InteractionField must consume that same baked truth. Skyglass and Verdant are original specs, not coastline copies; no noise terrain with props scattered on top.

Everything must remain inside the one HTML file, but organize the source into readable sections and small classes/functions for math, renderer, selected map architecture, world lifecycle, rigs, combat, skills, enemies, waves, VFX/audio, UI, and lifecycle; do not minify it into an unmaintainable blob. Pool transient objects, reuse typed arrays and WebGL resources, cap device pixel ratio, lights, particles, chains, fields, scars, enemies, and reaction writes, and add graceful quality reduction. Design toward stable 60 FPS on defined modern desktop hardware, but never claim it without measurement. Prefer a complete playable system with simplified procedural detail over omitted classes, fake buttons, screenshots, or a beautiful non-game scene. End your response with the complete `index.html`, not instructions for the user to assemble it.
```
